services:
  web:
    build: .
    ports:
      - "${FLASK_PORT:-5000}:5000"
    volumes:
      - uploads_data:/app/uploads
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1

volumes:
  uploads_data:
